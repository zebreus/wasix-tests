#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="$SCRIPT_DIR/.wasix-clang-bin"
SRC="$SCRIPT_DIR/wasix-clang.rs"
if [ ! -x "$BIN" ] || [ "$BIN" -ot "$SRC" ]; then
    rustc "$SRC" -o "$BIN"
fi
exec "$BIN" "$@"
